<h1>Photos</h1>

<%= content_for :sidebar_options do %>
    <p> Hola desde photos/index </p>
<%end%>

<% @photos.each do |photo| %>
    <div>
        <h2><%= photo.title %> </h2>
        <%= image_tag photo.image_url %>
        <%= link_to "Ver mas", photo %>
    </div>
<% end %>
<br><br><br><br>
<%= link_to "New photo", new_photo_path %>